/**
 * Write an expression that calculates rectangle’s area by given width and height.
 */
var width = 3.5,
    height = 4;

console.log('Area of rectangle is ' + getArea(width, height));

function getArea(w, h) {
    return w * h;
}